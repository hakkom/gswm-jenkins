/* Requires the Docker Pipeline plugIn */
pipeline {
  agent{ docker {imaage 'maven:3.9.4-eclipse-temurin-17-aalpine'}}
  stages{
    stage{'build'}
      steps{
        sh 'mvn --version'
      }
  }
}
